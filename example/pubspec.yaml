name: flutter_rx_base_example
description: Example app for flutter_rx_base.
publish_to: none
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ">=5.4.0 <6.0.0"
  rxdart: ">=0.27.0 <1.0.0"
  flutter_rx_base:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ">=3.0.0"

flutter:
  uses-material-design: true
